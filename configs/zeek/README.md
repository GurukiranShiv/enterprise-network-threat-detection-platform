# Zeek Config Placeholder

This project includes Zeek-style sample logs so it can run immediately. In a live lab, place exported Zeek logs into `data/sample/` using these names:

- `zeek_conn.log`
- `zeek_dns.log`
- `zeek_http.log`
