# Suricata Config Placeholder

This project includes a Suricata-style `eve.json` sample. In a live lab, place Suricata alert output into:

```text
data/sample/suricata_eve.json
```

The detection engine expects newline-delimited JSON events.
